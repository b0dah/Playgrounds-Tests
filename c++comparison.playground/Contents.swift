import Cocoa

let size = 4, rank = 0

let ar : [Any] = [1.2, 1.9]


var numbers: [Any] = [];
//for rank in 0...10 {
    if (rank%2==0) {
        if (rank == 0) {
            if (size%2==0) {
                fill()
                out()
            }
            else {
                var numbers: [Int] = [];
                fill()
                out
            }
        } else {
            var numbers: [Int] = [];
            fill
            out
        }
        
        print(numbers);
    }
//    else {
//        double numbers[n];
//        fillArray(*numbers, n);
//        cout << "Thread number: " << rank << " maximal element = " << arrayMax(numbers, n);
//    }

//}
